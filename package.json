{
  "name": "freshyx-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for the FRESHYX full-stack application, containing both the React frontend and the NestJS backend.",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "echo \"Please use your development environment's command to serve the frontend from the /frontend directory.\""
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
